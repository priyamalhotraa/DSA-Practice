```cpp
// Problem: Area of Circle
// Topic: Math

/* CH
APPROACH:
Find the area of a circle using its radius.

Use the formula:
    Area = π × r × r

Return the calculated area.
*/

class Solution {
public:
    double areaOfCircle(int r) {
        return M_PI * r * r;
    }
};
```
